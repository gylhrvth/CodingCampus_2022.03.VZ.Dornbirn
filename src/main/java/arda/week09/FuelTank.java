package arda.week09;

public class FuelTank extends Tank {
    public FuelTank(float tankCap, float currentFuelAmount, Car.FuelSource fuelSource) {
        super(tankCap, currentFuelAmount, fuelSource);
    }
}
